# Normalise line endings on commit; check out native on each platform.
# Without this, a Windows working copy commits CRLF and every diff looks like the
# whole file changed.
* text=auto eol=lf

# Binary — never touch these
*.png binary
*.jpg binary
*.pt  binary
*.pth binary
*.safetensors binary
*.gguf binary
